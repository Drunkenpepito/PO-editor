 <div class="container">
   <%= simple_form_for master_service do |f| %>
    <%= f.input :name,label: 'Master_service Name?'%>
   <!--  <%= f.input :photo, as: :file %> -->
    <%= f.submit "Submit ", class: "btn btn-primary" %>
  <% end %>
</div>
